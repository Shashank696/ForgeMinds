/**
 * ChatInput — Placeholder input with send button
 * Assigned to: DIL
 */
export default function ChatInput({ onSend, disabled }) {
  // TODO: Implement — DIL
  return (
    <div className="chatinput">
      <p>ChatInput placeholder</p>
    </div>
  );
}
