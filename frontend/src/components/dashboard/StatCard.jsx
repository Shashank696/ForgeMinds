/**
 * StatCard — Placeholder card
 * Assigned to: DIL
 */
export default function StatCard({ title, value, icon, trend }) {
  // TODO: Implement — DIL
  return (
    <div className="statcard">
      <p>StatCard placeholder</p>
    </div>
  );
}
